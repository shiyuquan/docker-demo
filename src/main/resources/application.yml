server:
  port: 9000

spring:
  application:
    name: docker-demo
  profiles:
#    active: dev
    active: '@maven.profile.active@'
  messages:
    basename: i18n/message
    encoding: UTF-8

logging:
  config: classpath:spring-logback.xml